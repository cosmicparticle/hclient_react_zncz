import React from 'react';
import CustomPage from "./CustomPage";

export default class DefCustomPage extends CustomPage{
    render(){
        return (
          <div>默认自定义页面</div>
        );
    }
}